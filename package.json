{
  "name": "pierwszy-gwizdek",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name pierwszy-gwizdek --branch master"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.13.5",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "wrangler": "^4.123.0"
  }
}
