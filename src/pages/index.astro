---
export const prerender = true;
import LandingPage from '../components/LandingPage.astro';
import { getLandingPage } from '../lib/strapi';

const content = await getLandingPage('published');
---

<LandingPage content={content} />
